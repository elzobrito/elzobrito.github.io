{
  "name": "elzobrito-hub",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "check": "astro check",
    "build": "astro build",
    "preview": "astro preview",
    "audit:public": "node scripts/audit-public.mjs",
    "audit:editorial": "node scripts/check-editorial-style.mjs",
    "test": "npm run check && npm run build && npm run audit:public && npm run audit:editorial"
  },
  "dependencies": {
    "@astrojs/mdx": "^4.3.6",
    "@astrojs/rss": "^4.0.12",
    "@astrojs/sitemap": "^3.6.0",
    "astro": "^5.14.1"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.5",
    "typescript": "^5.9.2"
  }
}
